~~ lineWidth: 30 ~~
== should format (skip) zts replaces TS enum — see tests/readme.md ==
enum Test {
    member,
    member2    =    5,
    "member3",
    "member4"="str"
}

[expect]
enum Test {
    member,
    member2 = 5,
    "member3",
    "member4" = "str",
}

== should format with initializer on next line if it goes over (skip) zts replaces TS enum — see tests/readme.md ==
enum Test {
    memberThatIsLong = 52364363463463,
    member2 = "some" + "test" + "withLotsOfThese"
}

[expect]
enum Test {
    memberThatIsLong =
        52364363463463,
    member2 = "some" + "test"
        + "withLotsOfThese",
}
