~~ enumDeclaration.trailingCommas: always ~~
== should format with trailing commas (skip) zts replaces TS enum — see tests/readme.md ==
declare enum Test
{
    member1    ,
    member2
}

[expect]
declare enum Test {
    member1,
    member2,
}
