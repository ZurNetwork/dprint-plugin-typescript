~~ enumDeclaration.bracePosition: maintain, lineWidth: 40 ~~
== should maintain the position for the brace position when on same line (skip) zts replaces TS enum — see tests/readme.md ==
enum Test {
}

[expect]
enum Test {
}

== should maintain the position for the brace position when on a new line (skip) zts replaces TS enum — see tests/readme.md ==
enum Test
{
}

[expect]
enum Test
{
}
