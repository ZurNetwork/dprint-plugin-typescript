~~ enumDeclaration.memberSpacing: maintain ~~
== should format keeping the spacing (skip) zts replaces TS enum — see tests/readme.md ==
declare enum Test {
    member1,

    member2,
    member3,

    /** test */
    member4,
}

[expect]
declare enum Test {
    member1,

    member2,
    member3,

    /** test */
    member4,
}
