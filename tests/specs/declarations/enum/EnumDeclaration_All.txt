== should format (skip) zts replaces TS enum — see tests/readme.md ==
enum Test
{
}

[expect]
enum Test {
}

== should format when const (skip) zts replaces TS enum — see tests/readme.md ==
const  enum Test
{
}

[expect]
const enum Test {
}

== should format with members (skip) zts replaces TS enum — see tests/readme.md ==
declare enum Test
{
    member1    ,
    member2
}

[expect]
declare enum Test {
    member1,
    member2,
}

== should format with only comments (skip) zts replaces TS enum — see tests/readme.md ==
enum Test {
    // testing
    /* test */
}
enum Test2 {//comment
}

[expect]
enum Test {
    // testing
    /* test */
}
enum Test2 { // comment
}

== should format when has comments (skip) zts replaces TS enum — see tests/readme.md ==
enum Test {
    // testing
    member,
    // testing
    member2,
    /* test */
    member3,
    /** test */
    member4,
}

[expect]
enum Test {
    // testing
    member,
    // testing
    member2,
    /* test */
    member3,
    /** test */
    member4,
}
